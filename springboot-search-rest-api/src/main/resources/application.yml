spring:
  datasource:
    url: jdbc:mysql://localhost:3306/searchapi
    username: root
    password: bode2001

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      ddl-auto: update
    show-sql: true
